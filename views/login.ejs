<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Log In</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      body {
        background-color: rgb(169, 243, 119);
        font-size: 1.5rem;
      }
      .container {
        padding: 20px;
        margin: 20px;
        height: 50%;
        width: 100%;
        border: 3px solid black;
        font-size: 16px;
        align-items: center;
        justify-content: center;
        background-color: rgb(236, 148, 65);
      }
      .input input {
        display: block;
        width: 100%;
        height: 40px;
        margin: 10px;
        padding: 10px;
      }
      .input label {
        font-size: 18px;
        font-weight: bold;
      }
      .btn {
        background-color: #4464bd; /* Green */
        border: none;
        color: white;
        margin: 10px;
        padding: 15px 32px;
        text-align: center;
        text-decoration: none;
        display: inline-block;
        font-size: 16px;
        width: 100%;
        font-weight: bold;
      }
    </style>
  </head>
  <body>
    <center><h1>LogIn Form</h1></center>
    <form action="/login" method="POST">
      <div class="container">
        <div class="input">
          <label for="loginId"> Email Or Username :</label>
          <input
            type="text"
            placeholder="Enter Email Or Username"
            name="loginId"
            required
          />
        </div>
        <div class="input">
          <label for="password">Password :</label>
          <input
            type="password"
            placeholder="Enter password "
            name="password"
            required
          />
        </div>
        <button class="btn" type="submit">Login</button>
      </div>
    </form>
  </body>
</html>
